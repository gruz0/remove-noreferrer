{
	"name": "gruz0/remove-noreferrer",
	"keywords": [ "phpunit" ],
	"require": {
		"php": ">=7.0.0"
	},
	"require-dev": {
		"phpunit/phpunit": "^7"
	},
	"scripts": {
		"test": [
			"vendor/bin/phpunit -v --debug --colors=always --no-coverage -c tests/phpunit/phpunit.xml"
		],
		"coverage": [
			"vendor/bin/phpunit -v --debug --colors=always -c tests/phpunit/phpunit.xml"
		]
	}
}
